# 迭代记录

本文件只记录会影响长期框架资产的变化：模板、样本、expected、识别规则、OCR 边界、输出契约和验证口径。

记录格式保持轻量：

```text
## YYYY-MM-DD

- 变化：
- 原因：
- 验证：
- 遗留缺口：
```

## 2026-06-01

- 变化：建立轻量长期文档入口，明确模板资产、Agent 输出契约和迭代记录口径；新增模板覆盖率报告命令。
- 原因：项目定位为长期迭代框架，需要持续记录资产可信度和演化规则，而不是按一次性功能目标收尾。
- 验证：新增覆盖率脚本测试；覆盖率报告可读取当前 `dhxy2_classic_pc` 模板包并暴露样本、expected 和可度量 anchor 缺口。
- 遗留缺口：真实固定窗口截图、expected JSON、真实 anchor/hash、OCR 引擎仍需后续补齐。

- 变化：将长期记忆文档合并到 `docs/`，保留单一文档目录。
- 原因：减少 `doc` 与 `docs` 双目录带来的入口分歧，让架构说明、参考图说明和长期框架记忆共用同一目录。
- 验证：更新 README 和项目 skill 中的文档路径引用。
- 遗留缺口：暂无。

- 变化：新增轻量 Git 提交编码规则，采用八位模块编号标题格式。
- 原因：当前项目需要长期按模块检索模板、样本、工具和文档演化历史，但不需要 Holmas 式强 hook 和自动收尾流程。
- 验证：规则文档明确模块编号表、标题类型、正文格式、归类规则和查号方式；项目总览已加入入口。
- 遗留缺口：自动校验已在后续补充 commit-msg hook；暂不维护编号登记表。

- 变化：为提交编码规则新增可测试的 commit-msg hook 校验。
- 原因：提交规则需要从文档约定升级为本地提交闸门，避免编号、标题类型和正文格式漂移。
- 验证：新增提交信息校验单元测试；安装脚本会设置 `core.hooksPath=.githooks`。
- 遗留缺口：暂不维护编号登记 JSON；流水号仍基于 git history 计算。
