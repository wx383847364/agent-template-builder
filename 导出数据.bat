@powershell -NoProfile -ExecutionPolicy Bypass -Command "$root='%~dp0'; Set-Location $root; New-Item -ItemType Directory -Force outputdata | Out-Null; $screenshot = if ('%~1' -eq '') { 'samples\dhxy2_classic_pc\screenshots\reward_popup__manual_summon_reward1.png' } else { '%~1' }; $py = if (Test-Path '.venv\Scripts\python.exe') { '.venv\Scripts\python.exe' } else { 'python' }; $env:PYTHONPATH = Join-Path $root 'src'; & $py -m agent_template_builder.pipeline.export_agent_rows $screenshot --fields-config agent_fields.json --pretty | Set-Content -Encoding utf8 outputdata\agent_rows.json; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; Write-Host ('导出完成：' + (Resolve-Path outputdata\agent_rows.json))"
@pause
